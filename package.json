{
  "name": "motion-x",
  "private": true,
  "version": "1.0.0",
  "devDependencies": {
    "lerna": "^3.19.0"
  },
  "scripts": {
    "publish": "lerna publish from-package --no-git-tag-version --no-push --no-verify-access --yes",
    "test": "lerna run test"
  },
  "workspaces": [
    "packages/*"
  ]
}
